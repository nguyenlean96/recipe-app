package ca.gbc.comp3095;

import ca.gbc.comp3095.models.*;

public class Main {
    public static void main(String[] args) {

        System.out.println("Data module initializing...");
        /*
        Cookbook cookBookent = context.getBean(Cookbook.class);
        Ingredient ingredientEnt = context.getBean(Ingredient.class);
        Mealplan mealPlan = context.getBean(Mealplan.class);
        Recipe recipeEnt = context.getBean(Recipe.class);
        User userEnt = context.getBean(User.class);
         */
    }
}